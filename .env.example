# Example Environment Configuration
# Copy this file to .env and fill in the values for your local setup if not using docker-compose defaults.

DATABASE_URL=postgresql://user:password@host:port/dbname
# DEBUG=False 